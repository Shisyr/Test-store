
<h2><%= @product.title %>
<span style="font-size: 14px;">
    <%= link_to 'Изменить', edit_product_path(@product) %>
    <%= link_to 'Удалить этот продукт', product_path(@product), method: :delete, data: {confirm: 'Are you sure?'} %>
</span>
</h2>
<%= image_tag @product.image(:large) %>
